# Env: API_KEY, UPSTREAM, UPSTREAM_PORT, PORT (Railway)
:{$PORT} {
	@ok {
		header X-API-Key {$API_KEY}
	}
	@deny not @ok

	respond @deny 401 {
		body "Unauthorized"
	}

	reverse_proxy @ok {$UPSTREAM}:{$UPSTREAM_PORT}
}
